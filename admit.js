import { db, collection, getDocs, query, where } from "./firebase.js";
const params=new URLSearchParams(location.search);const preset=params.get("id");if(preset)document.getElementById("admitId").value=preset;
document.getElementById("admitSearch").addEventListener("submit",async e=>{e.preventDefault();let id=document.getElementById("admitId").value.trim().toUpperCase(),out=document.getElementById("admitOutput");try{let s=await getDocs(query(collection(db,"registrations"),where("id","==",id)));if(s.empty){out.innerHTML='<div class="error">Registration ID not found.</div>';return}let r=s.docs[0].data();out.innerHTML=`<div class="student-card" id="printCard"><h2>MO OLYMPIAD — ADMIT CARD</h2><hr><p><b>Registration ID:</b> ${esc(r.id)}</p><p><b>Student:</b> ${esc(r.name)}</p><p><b>Class:</b> ${esc(r.class)}</p><p><b>School:</b> ${esc(r.school)}</p><p><b>City:</b> ${esc(r.city)}</p><p><b>Exam Date:</b> To be announced</p><p><b>Exam Time:</b> To be announced</p><p><b>Exam Centre:</b> To be announced</p><button class="btn primary" onclick="window.print()">🖨 Print / Save PDF</button></div>`}catch(x){out.innerHTML='<div class="error">Unable to load admit card.</div>'}});
function esc(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}