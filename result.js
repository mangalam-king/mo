import { db, collection, getDocs, query, where } from "./firebase.js";
const preset=new URLSearchParams(location.search).get("id");if(preset)document.getElementById("resultId").value=preset;
document.getElementById("resultSearch").addEventListener("submit",async e=>{e.preventDefault();let id=document.getElementById("resultId").value.trim().toUpperCase(),out=document.getElementById("resultOutput");try{let s=await getDocs(query(collection(db,"registrations"),where("id","==",id)));if(s.empty){out.innerHTML='<div class="error">Registration ID not found.</div>';return}let r=s.docs[0].data(),rs=await getDocs(query(collection(db,"results"),where("id","==",id)));if(rs.empty){out.innerHTML=`<div class="student-card"><h2>${esc(r.name)}</h2><p>Result has not been published yet.</p></div>`;return}let res=rs.docs[0].data(),pct=Math.round(Number(res.marks)/Number(res.total)*100);out.innerHTML=`<div class="result-card"><h2>${esc(r.name)}</h2><p><b>Registration ID:</b> ${esc(r.id)} &nbsp; <b>Class:</b> ${esc(r.class)}</p><div class="result-big">${esc(res.marks)} / ${esc(res.total)}</div><p style="text-align:center"><b>${pct}%</b></p><hr><p><b>Rank:</b> ${esc(res.rank||'—')}</p><p><b>Status:</b> ${esc(res.status)}</p><p><b>Certificate No.:</b> ${esc(res.certificate||'—')}</p></div>`}catch(x){out.innerHTML='<div class="error">Unable to load result.</div>'}});
function esc(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}